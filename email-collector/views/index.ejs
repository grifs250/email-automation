<!DOCTYPE html>
<html lang="lv">
<%- include('./partials/head.ejs') %>
<body>
  <div class="container">
    <header class="header">
      <div class="image-container">
        <img src="/images/physique.webp" alt="Header Image">
      </div>
    </header>
    <main>
      <div class="content">
        <section class="intro">
          <h1>Pacelies jaunā <span class="highlight">LĪMENĪ</span>!</h1>
          <h1>Kļūsti par cilvēku kuru citi <span class="highlight">respektē</span>.</h1>
        </section>
        <section class="subscribe">
          <hr class="line">
          <h2>Tava jaunā treniņprogramma - atslēga uz <span class="highlight">panākumiem</span>.</h2>
          <form action="/tnx" method="POST">
            <label for="name">Vārds:</label>
            <input type="text" id="name" name="name" placeholder="Ievadi savu vārdu" value="<%= typeof oldInput !== 'undefined' ? oldInput.name : '' %>" required>
            
            <label for="email">E-pasta adrese:</label>
            <input type="email" id="email" name="email" placeholder="Ievadi savu e-pastu" value="<%= typeof oldInput !== 'undefined' ? oldInput.email : '' %>" required>
            
            <button type="submit">Saņemt tagad</button>

            <% if (errors && errors.length > 0) { %>
                <div class="errors">
                    <% errors.forEach(function(error) { %>
                        <p><%= error.msg %></p>
                    <% }); %>
                </div>
            <% } %>

            <div class="info-icon-container">
              <span class="info-icon"><i class="fas fa-info-circle"></i></span>
              <span id="tooltip" class="tooltip-text">
                <p>Ievadi savu Vārdu, E-pastu, un es uzreiz Tev aizsūtīšu ē-pastā linku uz Treniņprogrammu.</p>
                <p>Tu tiksi iekļauts manā ēpastu sarakstā, kur, kad man atnāks iedvesma, atsūtīšu tev ko noderīgu.</p>
              </span>
            </div>

          </form>
        </section>
      </div>
      <%- include('./partials/footer.ejs') %>
    </main>
  </div>
</body>
</html>
